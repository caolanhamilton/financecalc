export type User = {
  first_name: string;
  second_name: string;
  email: string;
};

export type Application = {
  treatment_cost: number;
  deposit: number;
  loan_amount: number;
  monthly_payments: number;
  payment_length: number;
  apr: number;
};
